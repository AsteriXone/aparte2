<nav class="menubar navbar navbar-inverse navbar-fixed-top">
    <!--div class="container-fluid">
        <img style="max-height: 50px" class="img-responsive center-block" src="img/aparteLogo.jpg"/>
    </div-->
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <span id="header_shadow"></span>
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url('homepage') }}"> <img style="max-height: 50px" src="{{ asset('img/aparteLogo.jpg') }}"/></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if app.user %}
                    <!-- Menú para usuarios logueados -->
                    {% if is_granted('ROLE_ADMIN') %}
                        <!-- ROLE_ADMIN -->
                        <li><a href="{{ path('sonata_admin_redirect') }}">Administración</a></li>
                        <li><a href="{{ path('votaciones') }}">Votaciones</a></li>
                        {#<li><a href="{{ path('generar-citas') }}">Generar Citas</a></li>#}
                        <li><a href="{{ path('correo-masivo') }}">Correo masivo</a></li>
                        {#<li><a href="{{ path('usuario_cita') }}">Citas</a></li>#}
                        <!-- li><a href="#"></a></li>
                        <li><a href="#"></a></li -->
                        {% elseif is_granted('ROLE_USER') %}
                            <!-- ROLE_USER -->
                            <li><a href="{{ path('muestras') }}">Muestras</a></li>
                            <li><a href="{{ path('pedido-muestras') }}">Mi Pedido</a></li>
                            <li><a href="{{ path('votar-muestras') }}">Votar Orlas</a></li>
                            <li><a href="{{ path('votar-profe') }}">Votar Profes</a></li>
                            <li><a href="{{ path('usuario_cita') }}">Pedir Cita</a></li>
                            {#<li><a href="{{ path('incidencia') }}">Incidencia</a></li>#}
                            <li><a href="{{ path('orla-provisional') }}">Orla provisional</a></li>
                            <li><a href="{{ path('contactar') }}">Contactar</a></li>
                    {% endif %}
                {% else %}
                    <!-- Menú para usuarios por defecto -->
                    <li><a href="{{ path('orlas') }}">Orlas</a></li>
                    <li><a href="{{ path('fotoescuela') }}">Fotoescuela</a></li>
                    <li><a href="{{ path('graduaciones') }}">Graduaciones</a></li>
                    <li><a href="{{ path('estudio') }}">Estudio</a></li>
                    <li><a href="{{ path('galerias') }}">Galería</a></li>
                    <li><a href="{{ path('about') }}">Quienes Somos</a></li>
                    <li><a href="{{ path('contactar') }}">Contactar</a></li>
                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                 <li>
                     {% if app.user %}
                         <a href="{{ path('fos_user_security_logout') }}">
                             Cerrar Sesión
                             <span class="glyphicon glyphicon-log-out"></span>
                         </a>
                     {% else %}
                         <a href="{{ path('fos_user_security_login') }}">
                             Acceso
                             <span class="glyphicon glyphicon-user"></span>
                         </a>
                     {% endif %}
                </li>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>